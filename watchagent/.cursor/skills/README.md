Skills in this directory are executable Python scripts that query the
live database and return structured output. They are run directly:

  python .cursor/skills/data_analysis.py --question summary
  python .cursor/skills/replay_detection.py --n 48

Skills are written in Phase 9 after the DB schema is stable.
